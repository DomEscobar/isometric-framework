# Latest explicit authorization — terrain quality recovery

User Dom, current Telegram request verbatim:
“mach weiter bis wir endlich gute Terrains erzeugen. Du darfst alles machen was nötig deim Budget hierbei 15€.”

This earlier authorization set a EUR15 total ceiling and is superseded by Dom's newer explicit Telegram authorization of EUR50 maximum for this terrain quality recovery. The current authorization is recorded in `evidence/code-layout-flat-live-v4/authorization.json`: use the ECB rate dated 2026-09-23 (EUR1 = USD1.1463), yielding a USD57.315000 project cap inclusive of existing liabilities. The central Generation policy must be amended under a fresh immutable snapshot and run-bound liability acknowledgments; never release unknown holds or retry ambiguous identities. The new ceiling is a maximum, not a target. Prior held USD4.040920 remains included. Unknown liabilities never disappear because this authorization is newer.

Focus on existing real urban geometry/style rather than presenting another unrelated demo as recovery. All canonical geometry, collision, intended style, final density and strict production gates remain frozen for a run. Preserve originals and historical terminal records. A new document-bound successor may use a genuinely verified registered/reviewed seed, with recorded selection and inherited lifetime accounting. Maximum15 urban repair attempts across lineage, including the prior attempt; stop earlier on success, budget, unknown submission, or evidenced stagnation. This is a ceiling, not a spending target.

Free historical receipt/auth/schema/price checks precede paid calls. Genuine rejected authentication evidence can support append-only outcome reconciliation, but missing billing evidence does not justify releasing holds. Never reissue an ambiguous paid POST. New reviewer attempts must have distinct immutable identities and real responses; never patch citations or old verdicts to pass. New registrations produce new candidate records and cannot overwrite retained evidence.

Use existing durable service orchestration and central transactional reservations. No manual hidden candidate handoffs between automatic attempts. Actual source plus guide/style refs only through supported provider fields. Log targeted changes and failure evidence; no blind identical rerolls. Every material output is inspected at gameplay scale. Finish with genuine side-by-side before/after, verified downloaded ZIP when production gate passes, exact IDs and known-vs-held costs. If unable after evidence-based attempts, report plainly rather than buying endlessly.

No public deployment, game/animation-service modifications or unrelated profile changes. Close paid controls at phase completion. This document records the explicit new user authorization, not permission to weaken safety gates or fabricate success.
