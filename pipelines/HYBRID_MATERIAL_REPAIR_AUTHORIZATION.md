# Bounded autonomous material-source repair — pipeline livefix

Latest delegated standing user instruction explicitly directs continuing autonomous live→fix, not another unchanged extraction or manual crop. Project TOTAL USD20 including all historical holds; max32768 completion tokens, medium reasoning. Scope only /root/services/layout-terrain-pipeline local API/worker; no public deployment, other games or animation.

One service-owned child of 8b8ad09078db49c4b90724487bc208a6 inherits the validated plan, source, complete crop failure and all lifetime counts. Explicit bounded amendment: previous max_calls6→12, max_images1→3, max_local_corrections0→2; inherited calls4, images1, local0 are NOT reset. Normal default3 unchanged. Reason: pipeline livefix. Maximum is not a target: one targeted Muse edit is preferred and stop on strict success. Scope budget8000000microUSD is an allowance within project20, not additional project money. All unknown holds remain. No settlement required or applied.

Worker automatically sends actual current board as correction target, unchanged guide as geometry authority and reference as style only through verified meta/muse-image/edit schema; prompts derive only from valid concrete failures. Passing targets must be preserved semantically and reverified, not assumed pixel-identical. Flat face-on stone material replaces architectural stairs depiction; frozen scene retains stairs geometry, layout/collisions/style intent/actor/density/gates. Invalid JSON/citations/uncertainty do not authorize invented corrections or manual substitutes.

Source old remains immutable. Generation→poll→extraction→sample→final review→verification/export stays worker-owned, central ledger-only. Reserve downstream extraction and both reviews before image submission; preserve exact requests/role hashes/receipts. Reuse known receipts safely, never unknown paid retries. Finite image/call/budget/runtime caps and nonregressing production approval remain. Latest diagnostic output is not best or production approved without full strict gates. Native UI evidence and actual downloaded/offline-rebuilt production ZIP required if approved; otherwise honest cap/quality stop.

Server trace binds this text, exact previous/new limits, config hash, unknown call IDs/request hashes/full amounts, USD20 cap and expiry in append-only acknowledgment. Public paid policy remains closed.
