# Explicit project spending authorization

This document is historical authorization only. It does NOT authorize new paid work under the currently closed project policy. See `QUALITY_RECOVERY_AUTHORIZATION.md` for additional context; current live generation policy remains `approved=false`, total USD20 ceiling, and attempt count 10 over max_attempts 7. Any newer explicit user authorization must be bound to one fresh workflow configuration and recorded immutably in the central ledger before a paid request.

Original user authorization: “Budget Freigabe 10 usd los gogo”. It set USD10.00 TOTAL across the project, inclusive of liabilities, not additional per stage.

The user subsequently allowed a second urban style pilot using `/root/.hermes/cache/images/img_4a2bfeabebd9.jpg` as STYLE ONLY. That amendment raised the generation attempt count to 2 total; both attempts were consumed. It did not raise the shared USD10 cap.

All projects stages (planner, image generation, extraction and reviews) share one central ledger. Reserve before every paid request; retain ambiguous/receipt-less full holds, never blind-retry, preserve exact artifacts and stop when budget/cap ends. Accepted Waldlicht ground may be used as style-only input, never geometry. No public deployment, game changes, animation-service changes, or production/user acceptance is implied.

This historical authorization is superseded by neither an automatic retry nor unspent headroom. The central project policy currently states max_attempts=7 (exceeded: 10), approved=false, ceiling USD20. A newer direct user request may establish a fresh bounded authorization only for an exact new flow/run under that unchanged ceiling; it cannot retroactively settle or erase unknown calls.