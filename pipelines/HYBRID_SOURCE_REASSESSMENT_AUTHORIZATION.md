# Bounded retained-source reassessment — ONE live run

User (Dom) standing authorization 2026-09-22: continue and FINISH the source-reevaluation and run the pipeline to a terminal result. This document binds exactly ONE new service-owned child of sealed run `a3d4ce60a6db4bc7a2702217d62ce2db`, nothing else.

## What is authorized
- Re-evaluate ONE whole retained archived source (selected solely by service rule: newest archived source whose ONLY rejection is the proven architecture/material role contradiction `flat-material-role/2`) with the corrected extractor (`google/gemini-3.8-flash`, max_tokens 32768, reasoning medium). No image POST, no planner POST, no manual crops.
- Continue automatically only if the corrected extraction PASSES: sample scene (64px actor), sample review, final render, final review, production bundle. Any FAIL stops honestly at needs_attention.
- Up to 3 new model calls (extraction + both mandatory reviews), inheriting lifetime counters 8/12 calls and 3/3 images. max_images stays 3, max_calls stays 12.

## What is NOT authorized
- No new image purchase, no cap increase, no release or rewrite of any historical hold/verdict/receipt. Original verdict + receipt bytes stay immutable; the invalidation is a NEW versioned validator event only.
- No retry of the 2 historical unknown calls (full holds retained, never settled by this run). No public policy change. No game/animation changes, no public deployment.

## Budget
USD20 PROJECT TOTAL including every historical hold. Held now 10936256 microUSD. This run reserves at most 3 x 909312 = 2727936 microUSD in the existing central ledger before each paid boundary. All prior unknown/receipt-less holds stay fully reserved.
