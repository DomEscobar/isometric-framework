# Latest explicit user authorization — unattended two-style tests

User: Dom, Telegram579539601. Latest messages: “Ich gehe jetzt schlafen mache alles proaktiv ohne mich und Tests danach die Layout Generierung mit 2 verschiedenen styijg Referenzen”, followed by “Max Budget 15€”.

Execute proactively without per-attempt questions: finish free registration diagnosis on retained urban correction; afterwards exercise real layout/terrain generation with EACH of these two new image references:
- /root/.hermes/cache/images/img_6c8d5452390d.jpg — bright warm cream paving, restrained mint/green, coherent pixel clusters.
- /root/.hermes/cache/images/img_632abdd839ed.jpg — darker earthy fantasy cobbles, olive forest greens, shaded outlines; exclude HUD/text/watermark/characters/structures as terrain content.

Both references are STYLE/MATERIAL ONLY, never geometry authority. Preserve exact JPEG originals plus lossless once-decoded PNG lineage. Compare two style candidates on the SAME freshly generated valid supported flat layout for fair style comparison; also free layout regression on a second seed. Stay in this repository; no deployment or game/animation edits.

Budget maximum EUR15 TOTAL including ALL earlier charges and held liabilities, not EUR15 additional. Keep the existing stricter central USD10 ceiling rather than consuming the larger allowance. Prior starting ledger: 3 generation jobs holding USD0.033 +4 reviews holding USD3.184128 = USD3.217128. Never release unknown holds or count reported usage on top of holds. Since billing is USD, verify a current currency-rate safety margin before paid calls; internal conservative accounting may reserve EUR1.25 per USD and EUR2.50 extra fees/buffer under EUR15 while keeping USD10 cap. This is a conservative budgeting assumption, NOT a claimed live FX rate. If fresh evidence invalidates the margin, lower/stop spending rather than exceed EUR15.

Bound this phase to two new initial image tests, one per reference, and at most one evidence-driven repair per reference (max4 NEW image submissions, max7 total existing+new). This is a self-imposed smaller bound inside user's authorization, not a spending target. All reviewer calls use the SAME transactional ledger; use no new review when registration cannot safely produce final-density evidence. No ambiguous paid POST retry, no identical blind rerolls, no new confirmation per authorized step. Existing 15-repair lifetime ceiling cannot reset via restart. Old stopped run remains immutable; new explicit style experiments get their own lineage/records, not overwrite old run or old intended style.

Quality rules remain fail-closed. Do not relax the frozen2.5px old gate to pass prior output. New diagnostics may reveal estimator bias, but independent holdout/material contours must justify any improved algorithm; retain old evidence and version implementation. If geometry still fails, preserve source, report blocker, do not force registration/promotion. No manual hidden candidate handoff and no fabricated citations.

Deliver genuine tests/results/screenshots at gameplay scale when registered; otherwise clearly labelled unregistered diagnostics; actual downloaded ZIPs when valid; report exact IDs, known vs held costs, limits and unresolved quality. Disable paid controls at phase completion. No need to ask sleeping user routine questions.
