# One existing-source continuation: user-amended budget and output limit

Current explicit user authority supersedes the earlier USD10 financial ceiling:
- Exact quote: **„warte nein erhöhe das budget verdopple?“** — USD20 PROJECT TOTAL, including all historical costs and unknown liabilities; not USD20 extra.
- Exact quote: **„ja dann viell höhres Antwort-/Tokenlimit man“** — raise real OpenRouter max_tokens to32768 for extraction and both reviews; keep mandatory Gemini reasoning at its documented default medium, not low/disabled. Actual model supports65536 maximum. The32768 allowance includes reasoning AND answer, not a fabricated separate answer field.
- Standing instruction **„pipeline anpassen…live run->fix bis funktioniert“** covers this finite repair of the assistant-selected call cap: lifetime5→6, exactly3 inherited + at most3 new model calls, max1 image inherited, max0 local repairs. This is one amendment, not infinite retries or reset counters.

Continue only sealed d270dd95c2104370938199f8f91bcb55 via service-owned source/plan/receipt hash validation. Reuse unchanged provider original source-0.png and prediction55eaaa11559b4a53a56b96c18e825122. No new planner/image purchase, manual crops, fabricated truncated-JSON repair or geometry changes. Independent worker performs extraction→sample→review→final→review→export if every real gate permits. A genuine FAIL remains blocked.

New scoped budget is exactly conservative live extraction+two review holds using full context pricing plus32768 completion tokens each. Fresh metadata is rechecked before each POST; retain both old unknown calls at their full immutable reserves with an exact child-bound acknowledgment. No settlement is required or applied for launch; no old hold is released. Public generation/reviewer paid controls remain closed. Change only total_usd from10.00 to20.00 in central generation-policy.json; its approval/attempt/scope fields stay unchanged. Existing sealed config caps are historical, not rewritten.

Runtime, request, source, provider receipt, token policy, finite call amendment and acknowledgment are bound in new immutable child config/evidence. Success still needs native browser and offline downloaded-ZIP verification; owner visual acceptance remains pending. No public/game/animation deployment.
