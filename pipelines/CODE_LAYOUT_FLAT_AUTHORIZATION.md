# Current bounded live authorization — code-first flat terrain

Date: 2026-09-22. Owner approved in this Telegram turn:
- water interpretation: flat water surface with sandy shore; no depression/basin;
- start under exact smaller bounds: maximum 15 image cycles, 46 paid calls, $12.537070 remaining spend headroom, unchanged $20 project total, generation max_attempts 7→25; unresolved liabilities stay fully held/untouched.

Execution plan: code-generated geometry/layout first; require a live layout preview, inspect it before freezing; image-to-image edits only after that freeze; flat material source follows the frozen cell semantics; deterministic height renderer and independent reviews follow. No scene-first flow and no silently injected operator layout.

Bounded actual request: max_images=15, max_calls=46, max_seconds=7200, shared project total USD20.00, per-run reserve no greater than the current verified remaining headroom and full conservative priced call estimates. Current plan uses 32768 completion tokens / medium reasoning for Planner and Reviews, with one planner call, 15 image edits, and two reviews per candidate as the maximum model-call estimate. The run must stop earlier at any reserve, quote, liability, layout, semantic-alignment, provider-output, reviewer, artifact, or time gate. The model's image API does not guarantee exact canvas dimensions; an incompatible output is retained and rejected, with no automatic retry.

The earlier run c328c09a082c46fe958957837983f15d remains immutable and stopped: its completed planner receipt explicitly marked the bowl-shaped hollow unsupported. Do not resume it or rewrite its response. A new run describes only flat water with land shore. The exact set of two receipt-less historical calls remains fully reserved and acknowledged only by their IDs/request hashes/full holds in the fresh run binding; no release, settlement, reconciliation or retry is authorized for them.

This is not authority to exceed the $20 shared project ceiling, spend all headroom, increase image/API limits above the bounded values, remove liabilities, bypass strict validators, claim a numeric visual score absent evidence, deploy, or change games/services outside this workbench. The original 30-iteration request remains a maximum; the narrower 15-image cap is controlling.
