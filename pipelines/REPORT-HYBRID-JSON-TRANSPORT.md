# JSON-object transport: real provider success, local layout stop

New service run `a8d71015aaa64977af1bf73fc3240004` executed once through the independent systemd worker. OpenRouter/Gemini returned a successful completion `gen-1790081788-rtA2eCqsJFNoXBzFMPqd`, valid JSON and strict local Plan. Terminal needs_attention: `Doppelter Übergang`. No image purchased or production ZIP created; production download returns409. Run authorization removed after terminal readback.

Transport changes only: `response_format={"type":"json_object"}`; unchanged full Pydantic schema supplied in system prompt as local output contract. No strict provider schema compilation. Model, token cap8192, temperature0, user prompt and exact submitted actor/reference bytes remain unchanged. Transport identity is retained in exact ledger request; immutable config runtime_version hashes hybrid_models.py. Local Plan, crop bounds, extra-field prohibition, reference/source bindings and review citation gates remain unchanged.

Documented format: https://openrouter.ai/docs/api_reference/parameters#response-format and https://openrouter.ai/docs/api_reference/overview#structured-outputs . Historical HTTP400 private raw response says INVALID_ARGUMENT without naming a field. Success with changed transport supports the schema-envelope hypothesis; it does NOT identify an exact rejected schema keyword.

## Actual geometry result

Planner included each of three stair edges in both directions: (10,8)-(11,8), (10,9)-(11,9), (10,10)-(11,10). Compiler treats transitions as undirected and rejects reverse duplicates. A FREE diagnostic in-memory copy deduplicated the reverse edges and passed the entire unchanged compiler, revision `d6664a4ba7579eb40e86a07f08606d98225c05c24d1d64dd3a3570d4f9f3730a`. This is diagnosis only: original plan/run/receipt remain unchanged; no manual plan handed into service, no paid replay, no production approval. A future authorized receipt-replay path could explicitly canonicalize undirected edges with provenance/TDD instead of buying another planner, but none was implemented or run here.

## Budget and preservation

Before: central6541016 microUSD, remaining3458984. Live free metadata/auth/quote verified first full candidate3279608 (planner, extraction, sample review, final review, one image). New immutable acknowledgment binds BOTH exact prior unresolved calls/request hashes/full holds plus new run/config runtime hash. Max1image, max5calls, zero local corrections,1800s. No old receipt-less liability released or old policy modified.

Actual calls1/images0. New held817152; provider-reported actual cost USD0.022728, contained within the full hold, not added to it. Central held7358168; remaining2641832 microUSD. All prior rows checked field-for-field unchanged:10jobs,8reviews,8runs,2calls,40events. Old missing-receipt billing remains unsettled.

## Verification and files

RED transport assertion failed on old json_schema; GREEN targeted35passed; full isolated regression283passed (162 existing warnings), `evidence/hybrid-json-transport/{red,green,tests-full}.txt`. Includes transport invalid response controls, crop/schema/citation controls and full mocked autonomous pipeline; mocks are not live visual proof.

Evidence: `new-run.json`, `terminal-run.json`, `verification.json`, `transport-precheck.json`, `preflight.json`, `budget.json`, `authorization.json`. Private complete old-row snapshot `data/hybrid-json-before.json`0600. Exact request/receipt retained transactionally in central SQLite. No diagnostic-only paid call or repeat request.

Changed production source: hybrid_models.py. Updated exact-envelope test: tests/test_hybrid_error_evidence.py. Added one-use operator tool tools/start_json_hybrid_once.py and this report. Own API/worker restarted; no games, animation or public deployment touched.
