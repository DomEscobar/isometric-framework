# Explicit isolated static-character authorization

Latest explicit user message: **“ja generiere eine”**, after rejecting the locally hand-coded traveller. Delegated execution request: generate a real AI-provider character in `/root/services/layout-terrain-pipeline`, place it on unchanged village v4 terrain, provide native/closeup evidence and offline static-sprite navigation ZIP.

This authorizes ONE initial image attempt; maximum TWO only if a concrete inspected visual defect justifies a separately bound correction and budget permits. No animation, game/service deployment, external reviewer or terrain rerun. This run deliberately permits one attempt only. Final figure must derive from retained actual provider output, never a locally drawn substitute.

Budget remains EUR15 PROJECT TOTAL including historical charges and holds; existing stricter USD10 central Generation ledger ceiling is retained. Read live starting liabilities before execution. No new ledger, release, unknown retry or liability reset. Immutable new character binding is separate from all old terrain styles and runs.

Inputs: image1 is a blank square magenta canvas, only framing/background authority (no drawn figure); image2 is the complete supplied `img_994a0a414b3e.jpg`, decoded once to PNG, STYLE ONLY. Prompt requests one front-three-quarter down-right standing medieval traveller, warm tunic, muted teal short cape, brown hair, designed head/hands/boots and purposeful pixel clusters. No scene, text, sheet or baked shadow. Actual live Muse schema supports prompt/image_urls/aspect_ratio/output_format only; no invented alpha/mask parameters.

Preserve original bytes, immutable request/receipt/schema/quote and hashes. If returned backdrop is flat, a separately documented border-connected chroma cutout is permitted, preserving original. Derive native sprite directly once per inspected comparison from full-resolution cutout; target32–48px body at48×24 tiles. No terrain-source-replay production gate is claimed for a sprite. Parent Astra/user visual acceptance remains separate.

Scoped CLI policy is in-memory only, one extra attempt in the existing Generation ledger; public paid controls remain closed throughout. Close scoped policy in finally. Exact authorization SHA belongs in quote binding.
