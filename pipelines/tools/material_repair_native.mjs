import fs from 'node:fs';import path from 'node:path';import {createRequire} from 'node:module';
const require=createRequire(import.meta.url),base='http://127.0.0.1:48765',out='evidence/hybrid-material-repair',rid=JSON.parse(fs.readFileSync(out+'/new-run.json')).id;
const links='/root/.cache/ms-playwright/.links',candidates=fs.readdirSync(links).map(f=>fs.readFileSync(path.join(links,f),'utf8')).filter(p=>fs.existsSync(p+'/package.json'));const {chromium}=require(candidates[0]);
const browser=await chromium.launch({executablePath:'/root/.cache/ms-playwright/chromium_headless_shell-1234/chrome-linux/headless_shell',headless:true,args:['--no-sandbox']});
try{const page=await browser.newPage({viewport:{width:1920,height:1280},deviceScaleFactor:1});
for(let i=0;i<3;i++){const response=await page.goto(base+'/api/hybrid-runs/'+rid+'/sources/source-'+i+'.png');if(response.status()!==200)throw Error('source readback '+response.status());await page.evaluate(()=>{document.body.style.margin='0';document.body.style.background='#fff';let i=document.querySelector('img');i.style.cssText='display:block;position:absolute;left:0;top:0;width:1920px;height:1280px;max-width:none;';});await page.locator('img').screenshot({path:out+'/source-'+i+'-native-browser.png'});}
await page.setViewportSize({width:1440,height:1000});await page.goto(base);await page.evaluate(r=>localStorage.hybridRun=r,rid);await page.reload();await page.waitForFunction(()=>document.querySelector('#status').textContent.includes('needs_attention'));await page.locator('#status').screenshot({path:out+'/final-status.png'});await page.locator('#correction-progress').screenshot({path:out+'/final-correction.png'});await page.screenshot({path:out+'/final-ui.png',fullPage:true});await page.reload();await page.waitForFunction(()=>document.querySelector('#status').textContent.includes('needs_attention'));console.log('native source screenshots and terminal UI reload verified');
}finally{await browser.close()}
