# Explicit one-new-run authorization over retained unresolved liability

Latest explicit owner directive: “weiter?” followed by “ja was stehst du hier rum?” and the explicit implementation-and-execution task authorizes ONE independent new Hybrid LIVE run, not a resume/retry of `603c561c946e48efa2ba77a62e4b5848`.

This supersedes the former per-service no-exception rule ONLY for the new run bound by `hybrid_liability_authorizations`. It does not settle, reconcile, release or fabricate a receipt for the old request. All old rows and the full 817152 microUSD old Hybrid hold remain unchanged. Original project ceiling remains 10000000 microUSD, all historical liabilities included.

Operator records an immutable acknowledgment with exact old call IDs, request hashes, full amounts, exact new run ID/config hash, explicit approval text, project ceiling, run budget, lifetime image/call limits, expiry. No HTTP authorization endpoint or global bypass. Acknowledgment insertion is unique per new run and disallowed after its first call. SQL triggers prohibit modification/deletion. Before each new purchase the same transaction verifies all bound liabilities, unchanged configuration and authorization limits, the exact unknown-call set, central headroom and worker fence/cancellation. Any new or unrelated unknown call stops further purchases. Acknowledgment never grants old-call resubmission. Same-phase request drift is refused.

Free live auth/metadata/price check passed: planner and reviewer `google/gemini-3.8-flash`, conservative hold817152 each; Muse quote11000. First complete candidate requires planner+extraction+sample review+final review+one image =3279608 microUSD. Prior held total5723864 leaves4276136. New run is bounded at4276136, max1image, max6calls (at most one additional model review), max1local correction, max1800seconds. Model output limit remains8192; no holds/token reservation methods are reduced.

The last accepted description/reference/actor are reused as intent; planner must independently produce validated geometry and worker runs phases itself. Unknown or provider rejection stops with exact sanitized status and private request-bound evidence. No second paid new run is authorized by this document. Tests/evidence: `evidence/hybrid-scoped-attempt/`.
