# Lessons proposed for parent-owned procedural memory

No shared skills/configuration were edited by this worker. Parent should verify the delivered evidence before adding these to the existing terrain-service reference.

- Rasterize guides and categorical masks through pixel-center inverse projection, not independently filled polygons; shared-edge overdraw otherwise makes nominally disjoint masks overlap. Assert exact alpha partition for material AND connected-region masks.
- Publish clearance-aware cardinal adjacency as an export consumed by the technical UI. Validate each swept actor AABB, not just center-cell occupancy, and retain an explicit narrow-corridor test that distinguishes width 0.8 from 1.2.
- Keep PNG import declarations separate from observed image alignment. Exact dimensions and identical supplied manifest can only approve metadata, never painted shoreline/path compliance; preserve original bytes and report needs_attention until independent image review exists.
- Test mobile document width, not only full-page screenshot existence. A 64-character revision identifier caused a 390px viewport to export a 492px screenshot until overflow-wrap:anywhere was applied.
- When system Chromium fails before a tab opens, probe a discovered cached headless shell with existing Playwright core read-only. A package-install approval blocker need not block an already available toolchain; do not retry or bypass the blocked installation.
- Capture the actual ZIP download event, then reopen those exact bytes and verify CRC, internal hashes, canonical layout ID and mask semantics. A successful HTTP ZIP response alone does not establish correct export contents.
- Preserve final evidence in a fresh versioned directory after a code/semantic revision. Do not reuse a previous browser PASS after changing house access or layout generation; recreate the generated revision and real input journeys.
